ALTER TABLE `open_person`
	ADD COLUMN `receipt` VARCHAR(50) NOT NULL DEFAULT '0' AFTER `name`;

göra klart skicka kvitton
felmeddelande vid registrering om inte medlem
info till öppetansvariga
kalendergrej
kursbetalning
permissions, binär, user/responsible/admin

skapa öppetansvariga
scrolla till fel och success
visa info på min info
telefonnummer
dynamiska priser (hämtas via public api)
